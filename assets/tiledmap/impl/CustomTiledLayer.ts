import { _decorator, TiledLayer, TiledUserNodeData } from "cc";
const { ccclass } = _decorator;

@ccclass('CustomTiledLayer')
export class CustomTiledLayer extends TiledLayer {
    // ✅ 继承框架的所有功能
    // ✅ 重写你需要的方法
    // ✅ 添加新的功能

    
}